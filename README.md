# tmake
Tmake is a collection of Makefile includes to group common Makefile used by various projects such as [tash](https://github.com/tayoky/tash).

# install
To install the Makefiles on a project do
```sh
./tmake-install.sh path/to/project
```
